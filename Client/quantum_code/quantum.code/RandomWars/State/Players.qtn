struct DeckDice {
    int DiceId;
    int outGameLevel;
    int inGameLevel;
}

struct FieldDice {
    int DiceScale;
    int DeckDiceIndex;
}

component RWPlayer {
    player_ref PlayerRef;
    uint Team;
}

component DiceCreation {
    int Count;
}

component Sp {
    int CurrentSp;
    int CommingSp;
}

component Deck {
    int GuardianId;
    array<DeckDice>[5] Dices;
}

component Field {
    array<FieldDice>[15] Dices;
}