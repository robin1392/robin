#define PLAYERS_MAX 2

enum ModeType { Battle, Coop }
enum StateType { Battle, Preparation, Ready, Countdown, GameOver }

global
{
    Int32 Wave;
    FP RemainTime;
    Int32 SpWave;
    ModeType Mode;
    StateType State;
	array<RWPlayer>[PLAYERS_MAX] Players;
}